# .circleci/config.yml
version: 2.1

jobs:
  build:
    docker:
      - image: maven:3.9.0-jdk-21  # Imagen válida de Maven + JDK
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Build with Maven
          command: mvn clean package